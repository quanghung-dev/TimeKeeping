"use client";import{useMutation}from"@tanstack/react-query";import Link from"next/link";import{useSearchParams}from"next/navigation";import{useState}from"react";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{ApiError}from"@/lib/api/client";import{authService}from"@/services/auth-service";import{FormError}from"./form-error";
export function ResetPasswordForm(){const token=useSearchParams().get("token")??"";const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const mutation=useMutation({mutationFn:()=>authService.resetPassword(token,password)});const valid=/[a-z]/.test(password)&&/[A-Z]/.test(password)&&/\d/.test(password)&&/[^a-zA-Z0-9]/.test(password)&&password.length>=10&&password===confirm;const error=mutation.error instanceof ApiError?mutation.error.message:mutation.error?"Không thể đặt lại mật khẩu":!token?"Liên kết đặt lại mật khẩu không hợp lệ":null;if(mutation.isSuccess)return<div className="space-y-4 text-center"><p className="text-status-success">Mật khẩu đã được đặt lại.</p><Button asChild className="w-full"><Link href="/login">Đăng nhập</Link></Button></div>;return<form className="space-y-4" onSubmit={e=>{e.preventDefault();if(valid)mutation.mutate();}}><FormError message={error}/><div className="space-y-2"><Label htmlFor="newPassword">Mật khẩu mới</Label><Input id="newPassword" type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/><p className="text-xs text-muted-foreground">Ít nhất 10 ký tự, gồm chữ hoa, chữ thường, số và ký tự đặc biệt.</p></div><div className="space-y-2"><Label htmlFor="confirmPassword">Xác nhận mật khẩu</Label><Input id="confirmPassword" type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)}/>{confirm&&password!==confirm&&<p className="text-sm text-destructive">Mật khẩu xác nhận không khớp.</p>}</div><Button className="w-full" size="lg" disabled={!valid||mutation.isPending||!token}>{mutation.isPending?"Đang cập nhật…":"Đặt lại mật khẩu"}</Button></form>}
