import{describe,expect,it}from"vitest";import{optimisticAttendanceSnapshot}from"@/lib/attendance/optimistic";import type{AttendanceSnapshot}from"@/types/attendance";function empty():AttendanceSnapshot{return{workDate:"2026-07-15",timezone:"Asia/Ho_Chi_Minh",status:"not_started",schedule:{id:null,isWorkingDay:true,startAt:null,endAt:null,requiredMinutes:480},sessions:[],activeSession:null,activeBreak:null,totals:{grossWorkMinutes:0,breakMinutes:0,netWorkMinutes:0,roundedWorkMinutes:0,lateMinutes:0,earlyLeaveMinutes:0,missingMinutes:480,extraMinutes:0,overtimeMinutes:0},flags:[]};}describe("offline attendance projection",()=>{it("projects a full check-in/break/check-out sequence",()=>{let state=optimisticAttendanceSnapshot(empty(),"check-in","2026-07-15T01:00:00Z","1");expect(state.status).toBe("working");state=optimisticAttendanceSnapshot(state,"break-start","2026-07-15T05:00:00Z","2");expect(state.activeBreak).not.toBeNull();state=optimisticAttendanceSnapshot(state,"break-end","2026-07-15T06:00:00Z","3");expect(state.status).toBe("working");state=optimisticAttendanceSnapshot(state,"check-out","2026-07-15T10:00:00Z","4");expect(state.status).toBe("completed");expect(state.sessions[0]?.checkOutSource).toBe("offline");});it("does not mutate cached snapshot",()=>{const source=empty();optimisticAttendanceSnapshot(source,"check-in",new Date().toISOString(),"x");expect(source.sessions).toHaveLength(0);});});
