import type{AttendanceSnapshot}from"@/types/attendance";export function optimisticAttendanceSnapshot(snapshot:AttendanceSnapshot,action:string,at:string,requestId:string):AttendanceSnapshot{const next=structuredClone(snapshot);if(action==="check-in"){const session={id:`offline-${requestId}`,shiftId:null,checkInAt:at,checkOutAt:null,checkInSource:"offline",checkOutSource:null,note:null,breaks:[]};next.sessions.push(session);next.activeSession=session;next.status="working";}else if(action==="break-start"&&next.activeSession){const item={id:`offline-${requestId}`,breakType:"personal"as const,startedAt:at,endedAt:null,note:null};next.activeSession.breaks.push(item);next.activeBreak=item;next.status="on_break";}else if(action==="break-end"&&next.activeBreak){next.activeBreak.endedAt=at;next.activeBreak=null;next.status="working";}else if(action==="check-out"&&next.activeSession){next.activeSession.checkOutAt=at;next.activeSession.checkOutSource="offline";next.activeSession=null;next.status="completed";}return next;}
