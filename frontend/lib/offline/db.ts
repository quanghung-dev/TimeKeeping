import Dexie,{type EntityTable}from"dexie";export type OfflineActionType="check-in"|"check-out"|"break-start"|"break-end";export interface OfflineAction{id?:number;clientRequestId:string;type:OfflineActionType;localTimestamp:string;payload:Record<string,unknown>;syncStatus:"pending"|"syncing"|"failed";retryCount:number;lastError?:string;createdAt:string;}
class TimekeepingDb extends Dexie{outbox!:EntityTable<OfflineAction,"id">;constructor(){super("timekeeping-offline");this.version(1).stores({outbox:"++id,&clientRequestId,syncStatus,createdAt"});}}
export const offlineDb=new TimekeepingDb();export async function queueOfflineAction(type:OfflineActionType,payload:Record<string,unknown>={}){const localTimestamp=new Date().toISOString();const action:OfflineAction={clientRequestId:crypto.randomUUID(),type,localTimestamp,payload,syncStatus:"pending",retryCount:0,createdAt:localTimestamp};await offlineDb.outbox.add(action);return action;}
