import{z}from"zod";const money=z.string().regex(/^\d{1,13}(\.\d{1,2})?$/,"So tien khong hop le");export const payrollSettingsSchema=z.object({salaryType:z.enum(["monthly","hourly","daily"]),baseSalary:money,hourlyRate:money.nullable(),dailyRate:money.nullable(),weekdayOvertimeMultiplier:z.string().regex(/^\d+(\.\d{1,2})?$/),weekendOvertimeMultiplier:z.string().regex(/^\d+(\.\d{1,2})?$/),holidayOvertimeMultiplier:z.string().regex(/^\d+(\.\d{1,2})?$/),effectiveFrom:z.iso.date()}).superRefine((v,c)=>{if(v.salaryType==="hourly"&&!v.hourlyRate)c.addIssue({code:"custom",path:["hourlyRate"],message:"Can don gia theo gio"});if(v.salaryType==="daily"&&!v.dailyRate)c.addIssue({code:"custom",path:["dailyRate"],message:"Can don gia theo ngay"});});export const payrollPeriodSchema=z.object({year:z.coerce.number().int().min(2000).max(2200),month:z.coerce.number().int().min(1).max(12)});export const payrollPathSchema=z.object({year:z.coerce.number().int().min(2000).max(2200),month:z.coerce.number().int().min(1).max(12)});export const adjustmentSchema=z.object({adjustmentDate:z.iso.date(),adjustmentType:z.enum(["allowance","bonus","deduction"]),category:z.string().trim().min(1).max(50),amount:money,note:z.string().trim().max(1000).nullable().optional()});export type PayrollSettingsInput=z.infer<typeof payrollSettingsSchema>;export type AdjustmentInput=z.infer<typeof adjustmentSchema>;
