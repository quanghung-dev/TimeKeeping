import type{Pool}from"pg";export interface DailyReportRow{work_date:string;status:string;actual_work_minutes:number;standard_minutes:number;difference_minutes:number;overtime_minutes:number;missing_minutes:number;first_check_in:Date|null;last_check_out:Date|null;late_minutes:number;early_minutes:number;}
export class ReportRepository{static async daily(db:Pool,userId:string,start:string,end:string){return(await db.query<DailyReportRow>(`SELECT s.work_date::text,s.status,s.actual_work_minutes,s.standard_minutes,s.difference_minutes,s.overtime_minutes,s.missing_minutes,MIN(a.check_in_at) first_check_in,MAX(a.check_out_at) last_check_out,GREATEST(0,ROUND(EXTRACT(EPOCH FROM(MIN(a.check_in_at)-((s.work_date+ws.start_time)AT TIME ZONE u.timezone)))/60)-us.late_grace_minutes)::int late_minutes,GREATEST(0,ROUND(EXTRACT(EPOCH FROM(((s.work_date+ws.end_time+CASE WHEN ws.end_time<=ws.start_time THEN INTERVAL '1 day' ELSE INTERVAL '0 day' END)AT TIME ZONE u.timezone)-MAX(a.check_out_at)))/60)-us.early_leave_grace_minutes)::int early_minutes FROM v_daily_attendance_summary s JOIN users u ON u.id=s.user_id JOIN user_settings us ON us.user_id=u.id LEFT JOIN attendance_days d ON d.id=s.attendance_day_id LEFT JOIN work_schedules ws ON ws.id=d.schedule_id LEFT JOIN attendance_sessions a ON a.attendance_day_id=d.id WHERE s.user_id=$1 AND s.work_date BETWEEN $2 AND $3 GROUP BY s.attendance_day_id,s.work_date,s.status,s.actual_work_minutes,s.standard_minutes,s.difference_minutes,s.overtime_minutes,s.missing_minutes,ws.start_time,ws.end_time,u.timezone,us.late_grace_minutes,us.early_leave_grace_minutes ORDER BY s.work_date`,[userId,start,end])).rows;}
static async comparison(db:Pool,userId:string,year:number){return(await db.query(`SELECT EXTRACT(MONTH FROM work_date)::int month,COALESCE(SUM(actual_work_minutes),0)::int actual_minutes,COALESCE(SUM(standard_minutes),0)::int standard_minutes,COALESCE(SUM(overtime_minutes),0)::int overtime_minutes,COALESCE(SUM(missing_minutes),0)::int missing_minutes,COUNT(*) FILTER(WHERE status='completed')::int completed_days FROM v_daily_attendance_summary WHERE user_id=$1 AND EXTRACT(YEAR FROM work_date)::int=$2 GROUP BY month ORDER BY month`,[userId,year])).rows;}}
